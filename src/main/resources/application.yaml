spring:
  application:
    name: spring-ai-chatbots
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your-key}

server:
  port: 8081

logging:
  level:
    root: info
    org:
      springframework:
        ai: info

springdoc:
  swagger-ui:
    url: /swagger-ui.html